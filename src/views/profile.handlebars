<div>
    <h1>Tu perfil</h1>
    <p>{{user.first_name}} {{user.last_name}}</p>
    <p>{{user.email}}</p>
    <p>Edad: {{user.age}}</p>
    <p>Rol: {{user.rol}}</p>

    <a href="/api/session/logout"><button>Salir</button></a>
</div>